[settings]
default = price_monitor.settings

[deploy]
# Default target used by `scrapyd-deploy` in docker-entrypoint.sh. localhost is
# correct because the deploy runs *inside* the scrapyd container against its own
# daemon. Other clients reach it over the Docker network at http://scrapyd:6800.
url = http://localhost:6800/
project = price_monitor
